<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <artifactId>motech</artifactId>
        <groupId>org.motechproject</groupId>
        <version>0.29.1</version>
        <relativePath>../../../</relativePath>
    </parent>

    <artifactId>motech-scheduler</artifactId>
    <version>0.29.1</version>
    <packaging>bundle</packaging>

    <name>MOTECH Scheduler</name>

    <properties>
        <modules.root.dir>${basedir}/../../..</modules.root.dir>
    </properties>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>motech-platform-server-config</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.motechproject</groupId>
            <artifactId>motech-platform-osgi-extender-fragment</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>motech-platform-event</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>motech-scheduler-migration</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.quartz-scheduler</groupId>
            <artifactId>quartz</artifactId>
        </dependency>
        <dependency>
            <groupId>joda-time</groupId>
            <artifactId>joda-time</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.bval</groupId>
            <artifactId>org.apache.bval.bundle</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.servicemix.bundles</groupId>
            <artifactId>org.apache.servicemix.bundles.spring-context-support</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-lang</groupId>
            <artifactId>commons-lang</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-dbcp</groupId>
            <artifactId>commons-dbcp</artifactId>
        </dependency>
        <dependency>
            <groupId>com.googlecode.flyway</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <!-- Adds mysql/psql imports to the c3p0 pool, do not remove -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>motech-c3p0-fragment-bundle</artifactId>
        </dependency>
        <!-- Test -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>motech-pax-it</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.motechproject</groupId>
            <artifactId>motech-tasks</artifactId>
            <version>${project.version}</version>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>

        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <includes>
                    <include>task-channel.json</include>
                    <include>quartz.properties</include>
                </includes>
                <filtering>true</filtering>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <excludes>
                    <exclude>task-channel.json</exclude>
                    <exclude>quartz.properties</exclude>
                </excludes>
                <filtering>false</filtering>
            </resource>
        </resources>

        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
                <filtering>true</filtering>
            </testResource>
        </testResources>

        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>1.7</version>
                <executions>
                    <execution>
                        <phase>initialize</phase>
                        <goals>
                            <goal>parse-version</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <configuration>
                    <instructions>
                        <Context-Path>scheduler/api</Context-Path>
                        <Resource-Path>scheduler</Resource-Path>
                        <Blueprint-Enabled>true</Blueprint-Enabled>
                        <Export-Package>
                            org.motechproject.scheduler.builder;version=${project.version},
                            org.motechproject.scheduler.contract;version=${project.version},
                            org.motechproject.scheduler.exception;version=${project.version},
                            org.motechproject.scheduler.factory;version=${project.version},
                            org.motechproject.scheduler.service;version=${project.version},
                            org.motechproject.scheduler.tasks;version=${project.version}
                        </Export-Package>
                        <Import-Package>
                            org.apache.commons.dbcp,
                            org.eclipse.gemini.blueprint.config,
                            com.mysql.jdbc,
                            org.postgresql,
                            javax.ejb;resolution:=optional,
                            javax.jms;resolution:=optional,
                            javax.mail;resolution:=optional,
                            javax.mail.internet;resolution:=optional,
                            javax.management;resolution:=optional,
                            javax.management.openmbean;resolution:=optional,
                            javax.naming;resolution:=optional,
                            javax.rmi;resolution:=optional,
                            javax.servlet;resolution:=optional,
                            javax.servlet.http;resolution:=optional,
                            javax.sql;resolution:=optional,
                            javax.xml.bind;resolution:=optional,
                            javax.xml.namespace;resolution:=optional,
                            org.quartz;version="0",
                            commonj.*;resolution:=optional,
                            freemarker.*;resolution:=optional,
                            net.sf.cglib.core,
                            net.sf.cglib.proxy,
                            net.sf.cglib.reflect,
                            net.sf.jasperreports.*,
                            org.aopalliance.aop,
                            org.apache.velocity.*,
                            org.springframework.aop,
                            org.springframework.aop.framework,
                            org.springframework.cglib.core,
                            org.springframework.cglib.proxy,
                            org.springframework.cglib.reflect,
                            org.springframework.context.config,
                            org.springframework.jdbc.*;resolution:=optional,
                            org.springframework.security.config,
                            org.springframework.validation.beanvalidation,
                            org.springframework.web.servlet.config,
                            org.motechproject.config.service,
                            org.motechproject.commons.sql.service,
                            com.googlecode.flyway.core,
                            *
                        </Import-Package>
                        <!-- Spring context support has optional import of Quartz. This gives runtime exception.
                        So embedding quartz related packages. Fragment bundle may also solve the problem. -->
                        <Private-Package>
                            org.springframework.scheduling.quartz.*,
                            org.motechproject.scheduler.*,
                        </Private-Package>
                        <!--
                            flyway-core does not see sql files from the related directory when
                            it is run inside Felix framework. Probably the problem is with classpath
                            when flyway-core tries to find the correct sql file it searches inside
                            scheduler classpath instead of its classpath. Embedding sql
                            files inside scheduler resolves the problem. This instruction
                            should be removed when problem will be solved inside flyway-core.
                        -->
                        <Embed-Dependency>
                            org.springframework.context.support,
                            flyway-core;inline=com/googlecode/flyway/core/dbsupport/**/*.sql
                        </Embed-Dependency>
                        <Bundle-DocURL>
                            http://docs.motechproject.org/en/latest/modules/scheduler.html
                        </Bundle-DocURL>
                    </instructions>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-bundles</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${motech.dir}/bundles</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>target</directory>
                                    <includes>
                                        <include>*.jar</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <artifactId>maven-shade-plugin</artifactId>
                <version>1.7</version>
                <configuration>
                    <finalName>${project.artifactId}</finalName>
                    <outputDirectory>${project.build.directory}/standalone</outputDirectory>
                    <createDependencyReducedPom>false</createDependencyReducedPom>
                    <promoteTransitiveDependencies>true</promoteTransitiveDependencies>
                    <keepDependenciesWithProvidedScope>true</keepDependenciesWithProvidedScope>
                    <filters>
                        <filter>
                            <artifact>*:*</artifact>
                            <excludes>
                                <exclude>META-INF/*.SF</exclude>
                                <exclude>META-INF/*.DSA</exclude>
                                <exclude>META-INF/*.RSA</exclude>
                                <exclude>META-INF/spring.handlers</exclude>
                            </excludes>
                        </filter>
                    </filters>
                    <transformers>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                            <manifestEntries>
                                <Main-Class>org.motechproject.scheduler.service.MotechScheduler</Main-Class>
                            </manifestEntries>
                        </transformer>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.IncludeResourceTransformer">
                            <resource>META-INF/spring.handlers</resource>
                            <file>${basedir}/src/main/shade-config/spring.handlers</file>
                        </transformer>
                    </transformers>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>IT</id>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-clean-plugin</artifactId>
                        <version>2.5</version>
                        <inherited>false</inherited>
                        <executions>
                            <execution>
                                <id>pre-it-clean-config</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>clean</goal>
                                </goals>
                                <configuration>
                                    <excludeDefaultDirectories>true</excludeDefaultDirectories>
                                    <filesets>
                                        <fileset>
                                            <directory>${motech.dir}/config</directory>
                                            <includes>
                                                <include>${project.groupId}.${project.artifactId}/*.properties</include>
                                            </includes>
                                            <followSymlinks>false</followSymlinks>
                                        </fileset>
                                    </filesets>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
