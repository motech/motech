package org.motechproject.email.domain;

/**
 * The <code>DeliveryStatus</code> Enum contains all possible states, in which an email can exist.
 */

public enum DeliveryStatus {
    SENT,
    ERROR,
    RECEIVED
}
